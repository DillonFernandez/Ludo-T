package com.ludot.model;

// Names the two movement directions used after a piece leaves its home area.
public enum Direction {
    // Moves in the normal clockwise path.
    CLOCKWISE,

    // Moves in the opposite direction around the track.
    COUNTER_CLOCKWISE
}