package com.ludot.model;

// Labels the different square roles used when the board is assembled.
public enum CellType {
    STANDARD,
    STARTING_SQUARE,
    APPROACH,
    HOME_PATH,
    ALPHA,
    BETA,
    GAMMA,
    MYSTERY
}